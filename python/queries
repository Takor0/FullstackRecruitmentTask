All users with non-empty e-mail
User.objects.exclude(email='')

All discussions with more than 10 comments
Discussion.objects.annotate(comment_count=Count('comments')).filter(comment_count__gt=10)

All comments whose author has non-empty metadata.
Comment.objects.exclude(user__metadata={})


